---
title: "Model"
description: "Model composition and parameter management."
namespace: "Toro.NN"
---

Reflection-based parameter discovery, serialization, and deserialization for F# record models.

| Function | Description |
| --- | --- |
| `loadFromDict model tensors nameMap` | Load tensors from a dictionary into the model, matching by parameter name.
 When nameMap is Some, dictionary keys are translated before matching. |
| `loadInto model dirPath` | Load tensors from the given directory into the model in place. |
| `namedParams model` | Return all named tensors in the model via reflection. |
| `save model dirPath` | Save all model tensors to the given directory. |
| `trainableVars model` | Return all tensors that require gradients. |

## loadFromDict

```fsharp
loadFromDict model tensors nameMap
```

Load tensors from a dictionary into the model, matching by parameter name.
 When nameMap is Some, dictionary keys are translated before matching.

**Parameters**

- `model` : `'T`
- `tensors` : `Map<string, Tensor>`
- `nameMap` : `Map<string, string> option`

**Returns** `Result<unit, ToroError>`

---

## loadInto

```fsharp
loadInto model dirPath
```

Load tensors from the given directory into the model in place.

**Parameters**

- `model` : `'T`
- `dirPath` : `string`

**Returns** `Result<unit, ToroError>`

---

## namedParams

```fsharp
namedParams model
```

Return all named tensors in the model via reflection.

**Parameters**

- `model` : `'T`

**Returns** `(string * Tensor) list`

---

## save

```fsharp
save model dirPath
```

Save all model tensors to the given directory.

**Parameters**

- `model` : `'T`
- `dirPath` : `string`

**Returns** `Result<unit, ToroError>`

---

## trainableVars

```fsharp
trainableVars model
```

Return all tensors that require gradients.

**Parameters**

- `model` : `'T`

**Returns** `Tensor list`

