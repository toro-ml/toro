---
title: "Model"
description: "Model composition and parameter management."
namespace: "Toro.NN"
---

Attribute-driven model state discovery, serialization, and deserialization.
 Records use field names; options are transparent; tuples and stable lists use zero-based
 indices; union values use CaseName.index; and string dictionaries use ordinally sorted keys.
 Supported containers are records, options, tuples, discriminated unions, arrays, F# lists,
 ResizeArray/IList/IReadOnlyList, and string-keyed dictionaries.

| Function | Description |
| --- | --- |
| `loadFromDict model tensors nameMap mode` | Load tensors from a dictionary into the model, matching by canonical state name.
 When nameMap is Some, dictionary keys are translated before matching. |
| `loadInto model filePath mode` | Load tensors from a .safetensors file into the model in place.
 Validation completes before any model tensor is changed. |
| `namedBuffers model` | Return canonical persistent buffers in deterministic discovery order. |
| `namedParams model` | Return canonical parameters, including parameters that do not require gradients. |
| `namedState model` | Return parameters and buffers in deterministic discovery order. A shared Tensor is
 represented once, under the first path at which it is discovered. |
| `save model filePath` | Save canonical parameters and buffers to a .safetensors file. |
| `trainableParams model` | Return canonical parameters whose Tensor currently requires gradients. |

## loadFromDict

```fsharp
loadFromDict model tensors nameMap mode
```

Load tensors from a dictionary into the model, matching by canonical state name.
 When nameMap is Some, dictionary keys are translated before matching.

**Parameters**

- `model` : `'a`
- `tensors` : `Map<string, Tensor>`
- `nameMap` : `Map<string, string> option`
- `mode` : `LoadMode`

**Returns** `LoadReport`

---

## loadInto

```fsharp
loadInto model filePath mode
```

Load tensors from a .safetensors file into the model in place.
 Validation completes before any model tensor is changed.

**Parameters**

- `model` : `'T`
- `filePath` : `string`
- `mode` : `LoadMode`

**Returns** `LoadReport`

---

## namedBuffers

```fsharp
namedBuffers model
```

Return canonical persistent buffers in deterministic discovery order.

**Parameters**

- `model` : `'T`

**Returns** `NamedTensor list`

---

## namedParams

```fsharp
namedParams model
```

Return canonical parameters, including parameters that do not require gradients.

**Parameters**

- `model` : `'T`

**Returns** `NamedTensor list`

---

## namedState

```fsharp
namedState model
```

Return parameters and buffers in deterministic discovery order. A shared Tensor is
 represented once, under the first path at which it is discovered.

**Parameters**

- `model` : `'T`

**Returns** `NamedTensor list`

---

## save

```fsharp
save model filePath
```

Save canonical parameters and buffers to a .safetensors file.

**Parameters**

- `model` : `'T`
- `filePath` : `string`

---

## trainableParams

```fsharp
trainableParams model
```

Return canonical parameters whose Tensor currently requires gradients.

**Parameters**

- `model` : `'T`

**Returns** `NamedTensor list`

