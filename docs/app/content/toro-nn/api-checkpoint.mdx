---
title: "Checkpoint"
description: "Save and restore full training state (model + optimizer + epoch)."
namespace: "Toro.NN"
---

Save and restore versioned training state (model + optimizer + epoch).

| Function | Description |
| --- | --- |
| `load modelState optimizer dirPath` | Validate a Version 2 checkpoint completely, then commit model state,
 optimizer state, and learning rate in that order. Return the restored epoch. |
| `save modelState optimizer epoch dirPath` | Save canonical model state, optimizer state, and a Version 2 manifest. |

## load

```fsharp
load modelState optimizer dirPath
```

Validate a Version 2 checkpoint completely, then commit model state,
 optimizer state, and learning rate in that order. Return the restored epoch.

**Parameters**

- `modelState` : `ModelState`
- `optimizer` : `IOptimizer`
- `dirPath` : `string`

**Returns** `int`

---

## save

```fsharp
save modelState optimizer epoch dirPath
```

Save canonical model state, optimizer state, and a Version 2 manifest.

**Parameters**

- `modelState` : `ModelState`
- `optimizer` : `IOptimizer`
- `epoch` : `int`
- `dirPath` : `string`

