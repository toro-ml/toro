---
title: "GraphUtils"
description: "Graph utility functions."
namespace: "Toro.GNN"
---

| Function | Description |
| --- | --- |
| `addSelfLoops edgeIndex numNodes` | Add self-loops to edge index. Returns (newEdgeIndex, numNodes). |
| `degree index numNodes dtype device` | Compute node degree from an index vector (one row of edge_index). |

## addSelfLoops

```fsharp
addSelfLoops edgeIndex numNodes
```

Add self-loops to edge index. Returns (newEdgeIndex, numNodes).

**Parameters**

- `edgeIndex` : `Tensor`
- `numNodes` : `int`

**Returns** `Result<Tensor, ToroError>`

---

## degree

```fsharp
degree index numNodes dtype device
```

Compute node degree from an index vector (one row of edge_index).

**Parameters**

- `index` : `Tensor`
- `numNodes` : `int`
- `dtype` : `DType`
- `device` : `Device`

**Returns** `Result<Tensor, ToroError>`

