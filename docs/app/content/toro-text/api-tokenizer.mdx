---
title: "Tokenizer"
description: "Tokenizer wrapping Microsoft.ML.Tokenizers with F#-idiomatic API."
namespace: "Toro.Text"
---

| Function | Description |
| --- | --- |
| `fromBpe config` | Create from a BPE config. |
| `fromSentencePiece config` | Create from a SentencePiece config. |
| `fromTiktoken config` | Create from a Tiktoken config. |
| `fromWordPiece config` | Create from a WordPiece config. |
| `wrap inner` | Wrap an existing Microsoft.ML.Tokenizers.Tokenizer instance. |

## fromBpe

```fsharp
fromBpe config
```

Create from a BPE config.

**Parameters**

- `config` : `BpeConfig`

**Returns** `Tokenizer`

---

## fromSentencePiece

```fsharp
fromSentencePiece config
```

Create from a SentencePiece config.

**Parameters**

- `config` : `SentencePieceConfig`

**Returns** `Tokenizer`

---

## fromTiktoken

```fsharp
fromTiktoken config
```

Create from a Tiktoken config.

**Parameters**

- `config` : `TiktokenConfig`

**Returns** `Tokenizer`

---

## fromWordPiece

```fsharp
fromWordPiece config
```

Create from a WordPiece config.

**Parameters**

- `config` : `WordPieceConfig`

**Returns** `Tokenizer`

---

## wrap

```fsharp
wrap inner
```

Wrap an existing Microsoft.ML.Tokenizers.Tokenizer instance.

**Parameters**

- `inner` : `Tokenizer`

**Returns** `Tokenizer`

