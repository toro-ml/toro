---
title: "Tokenizer"
description: "Tokenizer wrapping Microsoft.ML.Tokenizers with F#-idiomatic API."
namespace: "Toro.Text"
---

| Function | Description |
| --- | --- |
| `fromBpe vocabPath mergesPath` | Create from BPE vocab and merges files. |
| `fromSentencePiece modelPath` | Create from a SentencePiece .model file. |
| `fromTiktoken modelName` | Create from a Tiktoken model name (e.g. "gpt-4o", "gpt-4").
 Requires the corresponding Microsoft.ML.Tokenizers.Data package. |
| `wrap inner` | Wrap an existing Microsoft.ML.Tokenizers.Tokenizer instance. |

## fromBpe

```fsharp
fromBpe vocabPath mergesPath
```

Create from BPE vocab and merges files.

**Parameters**

- `vocabPath` : `string`
- `mergesPath` : `string`

**Returns** `Tokenizer`

---

## fromSentencePiece

```fsharp
fromSentencePiece modelPath
```

Create from a SentencePiece .model file.

**Parameters**

- `modelPath` : `string`

**Returns** `Tokenizer`

---

## fromTiktoken

```fsharp
fromTiktoken modelName
```

Create from a Tiktoken model name (e.g. "gpt-4o", "gpt-4").
 Requires the corresponding Microsoft.ML.Tokenizers.Data package.

**Parameters**

- `modelName` : `string`

**Returns** `Tokenizer`

---

## wrap

```fsharp
wrap inner
```

Wrap an existing Microsoft.ML.Tokenizers.Tokenizer instance.

**Parameters**

- `inner` : `(type)`

**Returns** `Tokenizer`

